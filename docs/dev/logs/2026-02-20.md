# 2026-02-20

## [--:--] schedules — materialize template resources into scheduled activities
- **Files:** `convex/schema.ts`, `convex/lib/materializeQuantity.ts`, `convex/cultivationSchedules.ts`, `convex/scheduledActivities.ts`, `hooks/use-activity-execution.ts`, `components/activities/activity-schedule.tsx`
- **Why:** Scheduled activities had no resource data — `required_materials` was always `[]`. Template resources were only referenced indirectly. Now resources are materialized with calculated quantities (per_plant, per_m2, fixed) at schedule generation time, enabling pre-filled execution forms and resource count badges.
- **Commit:** `dfc5800`

## [--:--] templates — add duration_type/duration_value to activity templates
- **Files:** `convex/schema.ts`, `convex/activityTemplates.ts`
- **Why:** Activity templates only had `estimated_duration_minutes`. Added `duration_type` ("hours"|"days") and `duration_value` to distinguish multi-day activities (e.g. trimming) from hour-based ones (e.g. monitoring), used in phase schedule rendering.
- **Commit:** `f1528a1`

## [--:--] templates — replace create modal with wizard navigation
- **Files:** `components/templates/template-list.tsx`
- **Why:** "Crear" button now navigates to `/templates/new` wizard instead of opening a modal. Removed TemplateCreateModal import and state.
- **Commit:** `60beb23`

## [--:--] templates — creation wizard with 2-step flow
- **Files:** `app/(dashboard)/templates/new/page.tsx`, `components/templates/template-create-wizard.tsx`, `components/templates/wizard-step-basic.tsx`, `components/templates/wizard-step-phases.tsx`, `components/templates/phase-create-dialog.tsx`
- **Why:** New wizard at `/templates/new`: Step 1 (basic data + auto-fill phases from crop_type.default_phases), Step 2 (phases with drag & drop reorder via @dnd-kit/sortable). Creates template then phases sequentially, redirects to detail.
- **Commit:** `1888e49`

## [--:--] templates — rewrite template detail page
- **Files:** `app/(dashboard)/templates/[id]/page.tsx`
- **Why:** Replaced 3 info cards and inline activity lists with cleaner layout: single info card grid, clickable phase cards with activity count badges by type, timeline bar. Phases navigate to `/templates/[id]/phases/[phaseId]`.
- **Commit:** `6e626df`

## [--:--] templates — phase detail with day schedule and backend enrichment
- **Files:** `app/(dashboard)/templates/[id]/phases/[phaseId]/page.tsx`, `components/templates/phase-detail-view.tsx`, `components/templates/add-activity-dialog.tsx`, `convex/templatePhases.ts`, `convex/templateActivities.ts`
- **Why:** New phase detail page with day-by-day schedule (Día 1..N), activity cards with type colors, multi-day rendering with "Día X/N" badges, add activity dialog (type → template → pre-fill → save). Backend enriched getById with duration_type/value + resource counts; createFromActivityTemplate now accepts startDay.
- **Commit:** `ade1fc3`
