# 2026-02-20

## [--:--] schedules — materialize template resources into scheduled activities
- **Files:** `convex/schema.ts`, `convex/lib/materializeQuantity.ts`, `convex/cultivationSchedules.ts`, `convex/scheduledActivities.ts`, `hooks/use-activity-execution.ts`, `components/activities/activity-schedule.tsx`
- **Why:** Scheduled activities had no resource data — `required_materials` was always `[]`. Template resources were only referenced indirectly. Now resources are materialized with calculated quantities (per_plant, per_m2, fixed) at schedule generation time, enabling pre-filled execution forms and resource count badges.
- **Commit:** `dfc5800`

## [--:--] templates — add duration_type/duration_value to activity templates
- **Files:** `convex/schema.ts`, `convex/activityTemplates.ts`
- **Why:** Activity templates only had `estimated_duration_minutes`. Added `duration_type` ("hours"|"days") and `duration_value` to distinguish multi-day activities (e.g. trimming) from hour-based ones (e.g. monitoring), used in phase schedule rendering.
- **Commit:** `f1528a1`

## [--:--] templates — replace create modal with wizard navigation
- **Files:** `components/templates/template-list.tsx`
- **Why:** "Crear" button now navigates to `/templates/new` wizard instead of opening a modal. Removed TemplateCreateModal import and state.
- **Commit:** `60beb23`

## [--:--] templates — creation wizard with 2-step flow
- **Files:** `app/(dashboard)/templates/new/page.tsx`, `components/templates/template-create-wizard.tsx`, `components/templates/wizard-step-basic.tsx`, `components/templates/wizard-step-phases.tsx`, `components/templates/phase-create-dialog.tsx`
- **Why:** New wizard at `/templates/new`: Step 1 (basic data + auto-fill phases from crop_type.default_phases), Step 2 (phases with drag & drop reorder via @dnd-kit/sortable). Creates template then phases sequentially, redirects to detail.
- **Commit:** `1888e49`

## [--:--] templates — rewrite template detail page
- **Files:** `app/(dashboard)/templates/[id]/page.tsx`
- **Why:** Replaced 3 info cards and inline activity lists with cleaner layout: single info card grid, clickable phase cards with activity count badges by type, timeline bar. Phases navigate to `/templates/[id]/phases/[phaseId]`.
- **Commit:** `6e626df`

## [--:--] cultivars — productos derivados por cultivar (US-1 a US-6)
- **Files:** `convex/schema.ts`, `convex/products.ts`, `convex/cultivars.ts`, `app/(dashboard)/cultivars/[id]/page.tsx`, `components/cultivars/cultivar-form.tsx`, `components/cultivars/cultivar-create-modal.tsx`, `components/cultivars/cultivar-list.tsx`, `components/cultivars/cultivar-products-tab.tsx`, `components/cultivars/create-phase-product-modal.tsx`, `components/products/product-table.tsx`
- **Why:** Cada cultivar ahora puede tener productos de catalogo asociados por fase del crop_type. Schema: `cultivar_id` + `crop_phase` en products con indexes. Backend: query `getProductsByPhase`, auto-chain de `transformation_produces_id`, query `getByCultivarPhase` para transiciones. UI: detalle cultivar con tabs (Info + Productos), wizard de creacion con seccion de productos por fase con toggles, badge "Cultivar" en tabla de productos.
- **Commits:** `ce3b986`, `d1a0168`, `8a3d9bc`, `84f04fc`

## [--:--] templates — phase detail with day schedule and backend enrichment
- **Files:** `app/(dashboard)/templates/[id]/phases/[phaseId]/page.tsx`, `components/templates/phase-detail-view.tsx`, `components/templates/add-activity-dialog.tsx`, `convex/templatePhases.ts`, `convex/templateActivities.ts`
- **Why:** New phase detail page with day-by-day schedule (Día 1..N), activity cards with type colors, multi-day rendering with "Día X/N" badges, add activity dialog (type → template → pre-fill → save). Backend enriched getById with duration_type/value + resource counts; createFromActivityTemplate now accepts startDay.
- **Commit:** `ade1fc3`

## [--:--] production — rebuild production module with calendar activities tab
- **Files:** `app/(dashboard)/production/page.tsx`, `components/production/activities-tab.tsx`, `components/production/activity-calendar.tsx`, `components/production/calendar-*.tsx`, `components/production/activity-detail-page.tsx`, `components/production/schedule-activity-wizard.tsx`, `components/production/wizard-step-*.tsx`, `components/production/edit-activity-wizard.tsx`, `convex/scheduledActivities.ts`, `convex/schema.ts`
- **Why:** Replaced old production page (phase board + analytics + orders) with new 3-tab layout. Implemented Actividades tab with custom calendar (day/week/month views), activity detail page, 4-step new activity wizard (area → batches → template → review with editable resources), 2-step edit wizard. Added `getById` query and `update` mutation. Fixed `createManual` to materialize resources into `scheduled_activity_resources`. Deleted `/scheduled-activities` page.
- **Commit:** `5a1c84d`

## [23:58] production — orders tab with stats and list
- **Files:** `components/production/orders-tab.tsx`, `components/production-orders/production-order-list.tsx`, `app/(dashboard)/production/page.tsx`
- **Why:** Created OrdersTab with CompactStats (4 cards: Activas, Planificacion, Completadas, Progreso) + ProductionOrderList. Replaced "Nueva Orden" modal with navigation to `/production/orders/new`. Wired into production page tab ordenes.
- **Commit:** `9b96e39`

## [--:--] cultivars — refactor create modal to wizard + new SKU format
- **Files:** `components/cultivars/cultivar-create-wizard.tsx`, `components/cultivars/wizard-step-basic.tsx`, `components/cultivars/wizard-step-products.tsx`, `app/(dashboard)/cultivars/new/page.tsx`, `components/cultivars/cultivar-list.tsx`, `components/cultivars/cultivar-form.tsx`
- **Why:** Replaced modal-based creation with full-page wizard at `/cultivars/new` (2 steps: basic info + phase products). New SKU format: cultivar initials + sequential number (e.g. `BLDR-001`) instead of name+phase. Deleted `cultivar-create-modal.tsx`, cleaned phase product code from `cultivar-form.tsx` (now edit-only).
- **Commit:** `8fb751b`
