# 2026-02-24

## [10:00] seed — repoblar seed completo para entorno de pruebas
- **Files:** `convex/seedOnboardingData.ts`, `convex/seedReseed.ts`
- **Why:** El seed solo generaba config (areas, cultivars, products, template). Ahora el template incluye completion_criteria, phase_role, distribution_strategy, y type_id. El reseed ademas crea 2 ordenes de produccion demo (1 activa mid-lifecycle con 2 fases completadas, 1 en planning), 8 plant material products, y phase_transition_log entries.
- **Commit:** pendiente

## [14:30] seed — enriquecer reseed con trazabilidad completa
- **Files:** `convex/seedReseed.ts`, `convex/templatePhases.ts`, `convex/schema.ts`
- **Why:** El reseed avanzaba fases via `completePhase` (admin override), dejando vacías las tablas de trazabilidad. Ahora usa `executeActivity` sobre entry/exit SAs para generar registros reales en `activities`, `phase_transition_log`, `activity_resources`. Agrega datos directos en `activity_observations`, `activity_environmental_readings`, y `batch_movements` via nuevas internalMutations.
- **Cambios clave:**
  - Reemplazado `completePhase` por `executeActivity` en entry/exit SAs + `updateCriterionStatus`
  - Actividades regulares (Riego, Fertilización) ejecutadas ad-hoc con `typeId` + `batchIds`
  - 5 nuevas funciones: `getScheduledActivitiesForPhase`, `getBatchesForOrder`, `seedTraceabilityData`, `seedBatchMovements`, expanded `getSeededReferences`
  - Fix bug: `templatePhases.ts` defaulteaba `completion_criteria: {}` en vez de `[]`
- **Resultado:** 20 activities, 14 activity_resources, 5 observations, 15 env readings, 4 batch_movements, 8 phase_transition_log
- **Commit:** `ed8309e`
