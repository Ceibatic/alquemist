// DEPRECATED: Clerk auth config
// Now using custom session-based authentication
// See convex/auth.ts and convex/registration.ts for the new auth system
const authConfig = {
  providers: [],
};

export default authConfig;